services:
  group-calendar:
    build:
      context: .
      network: host
    container_name: group-calendar
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./data:/app/data
