services:
  group-calendar:
    build: .
    container_name: group-calendar
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
