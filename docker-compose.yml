services:
  group-calendar:
    build:
      context: .
      network: host
    network_mode: host
    container_name: group-calendar
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./data:/app/data
