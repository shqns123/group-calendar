@AGENTS.md
1. 현재 그룹일정 및 특근 관리하는 캘린더를 만들었다.
2. 현재 서버는 총3개 local에서 npm run dev서버, 시놀로지나스 container manager로 구동중인 test서버와 main서버
3. test서버는 docker-compose.test.yml로 빌드함
4. main서버는 docker-compose.yml로 빌드함