services:
  group-calendar-test:
    build:
      context: .
      network: host
    container_name: group-calendar-test
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env.test
    volumes:
      - ./data:/app/data
